import { GaClickDirective } from './ga-click.directive';

describe('GaClickDirective', () => {
  it('should create an instance', () => {
    const directive = new GaClickDirective();
    expect(directive).toBeTruthy();
  });
});
